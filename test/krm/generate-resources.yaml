apiVersion: viaduct.ai/v2
kind: ksops
metadata:
  name: ksops-single-file-generator
  annotations:
    config.kubernetes.io/function: |
        exec:
          # path should be relative to manifest files
          # if the binary is your PATH, you can do 
          # path: ksops
          path: ../../ksops
files:
  - ./secret.enc.yaml

